#include "stdio.h"
#include "Plateau.h"



int main(){
    int x, y;
    Tuile Plateau[12][26];
    initialiserPlateau(Plateau);
    afficherPlateautest(Plateau);
    printf("x");
    scanf("%d", &x)
    printf("y");
    scanf("%d", &y)
    ajouterTuile(Plateau,x,y,couleur, M)
    return 0;
}